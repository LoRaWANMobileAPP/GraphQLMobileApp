subscription SignalAddedSubscription($where: SignalSubscriptionFilterInput) {
    signalAdded(where: $where) {
        unit
        createdAt
        data {
            rawValue
            numericValue
        }
    }
}